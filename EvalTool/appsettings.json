{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "EvalTool": "Information"
    }
  },
  "_README_GeminiApiKey": "GeminiApiKey is intentionally empty in this committed file. EvalTool reads the real value from (a) appsettings.Local.json (gitignored) or (b) the GeminiApiKey environment variable. See EvalTool/HELDOUT_RUNBOOK.md.",
  "GeminiApiKey": "",
  "MlApi": {
    "BaseUrl": "http://localhost:8000",
    "ApiKey": "",
    "TimeoutSeconds": 30,
    "ScoringTimeoutSeconds": 120
  }
}
